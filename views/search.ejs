<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link rel="stylesheet" href="/main.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
</head>
<body class="grey-bg">

  <%- include('nav.ejs') %>

  <span>검색결과</span>
  <div class="white-bg">
    <% for (let i = 0; i < 검색결과.length; i++){ %>
    <div class="list-box">
      <a href="/detail/<%= 검색결과[i]._id %>"><%= 검색결과[i].title %>
        <a href="/edit/<%= 검색결과[i]._id %>">✏️</a>      
        <span data-id="<%= 검색결과[i]._id %>" class="delete">🗑️</span>
      </a>
      <p><%= 검색결과[i].content %></p>
    </div>
    <% } %>
  </div> 
  
</body>
</html>